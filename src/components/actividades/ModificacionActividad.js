import { Box, Typography } from '@material-ui/core';

export default function ModificacionActividad() {
  return (
    <Box mt={8}>
      <Typography variant="h4" color="primary">
        Editar actividad
      </Typography>
    </Box>
  );
}
