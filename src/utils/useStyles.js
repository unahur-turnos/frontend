import { makeStyles } from '@material-ui/core';

export const useStyles = () =>
  makeStyles((theme) => ({
    drawerContainer: {
      padding: '20px 30px',
    },
    tamanioLogo: {
      width: '55px',
      height: '42px',
    },
    tamanioFoto: {
      height: '45px',
    },
  }));
